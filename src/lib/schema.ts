export interface UserInfo {
	id: string;
	accountId: string;
	name: string;
	avatarHash: string;
	groupsId: string[];
}

export interface AccountInfo {
	id: string;
	name: string;
	avatarHash: string;
}
