const config = {
	clientId: process.env.GAS_CLIENT_ID,
	clientSecret: process.env.GAS_CLIENT_SECRET,
	refreshToken: process.env.GAS_REFRESH_TOKEN,
	scriptId: process.env.GAS_SCRIPT_ID,
};

export default config;
